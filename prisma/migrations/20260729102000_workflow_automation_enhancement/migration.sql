-- CreateEnum
CREATE TYPE "AssignmentStrategy" AS ENUM ('ROUND_ROBIN', 'LEAST_WORKLOAD', 'MANUAL_OVERRIDE');

-- AlterTable
ALTER TABLE "workflows" ADD COLUMN "isAutoGenerated" BOOLEAN NOT NULL DEFAULT false,
ADD COLUMN "metadata" JSONB;

-- AlterTable
ALTER TABLE "tasks" ADD COLUMN "serviceId" TEXT,
ADD COLUMN "serviceName" TEXT,
ADD COLUMN "departmentId" TEXT,
ADD COLUMN "priority" TEXT NOT NULL DEFAULT 'MEDIUM',
ADD COLUMN "dueDate" TIMESTAMP(3),
ADD COLUMN "description" TEXT,
ADD COLUMN "isCustom" BOOLEAN NOT NULL DEFAULT false,
ADD COLUMN "templateKey" TEXT;

-- AlterTable
ALTER TABLE "services" ADD COLUMN "departmentId" TEXT,
ADD COLUMN "taskTemplates" JSONB;

-- CreateTable
CREATE TABLE "workflow_settings" (
    "id" TEXT NOT NULL,
    "companyId" TEXT NOT NULL,
    "assignmentStrategy" "AssignmentStrategy" NOT NULL DEFAULT 'LEAST_WORKLOAD',
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,

    CONSTRAINT "workflow_settings_pkey" PRIMARY KEY ("id")
);

-- CreateIndex
CREATE UNIQUE INDEX "workflow_settings_companyId_key" ON "workflow_settings"("companyId");

-- CreateIndex
CREATE INDEX "tasks_workflowId_serviceId_idx" ON "tasks"("workflowId", "serviceId");

-- CreateIndex
CREATE INDEX "tasks_companyId_departmentId_status_idx" ON "tasks"("companyId", "departmentId", "status");

-- CreateIndex
CREATE INDEX "services_companyId_departmentId_idx" ON "services"("companyId", "departmentId");

-- AddForeignKey
ALTER TABLE "workflow_settings" ADD CONSTRAINT "workflow_settings_companyId_fkey" FOREIGN KEY ("companyId") REFERENCES "companies"("id") ON DELETE CASCADE ON UPDATE CASCADE;

-- AddForeignKey
ALTER TABLE "services" ADD CONSTRAINT "services_departmentId_fkey" FOREIGN KEY ("departmentId") REFERENCES "departments"("id") ON DELETE SET NULL ON UPDATE CASCADE;
